/* ===============================================================================
=> SEXY BUTTONS THEME NAMED "<%= theme_name %>"
=============================================================================== */
.sexy-button-<%= theme_name %> { 
 	background-image:url(bg.png);
	font-size:<%= font_size %>;
}
.sexy-button-<%= theme_name %> span { 
	height:<%= height %>; 
	line-height:<%= height %>;
	background-image:url(bg.png);
}