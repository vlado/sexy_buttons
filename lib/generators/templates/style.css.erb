/* ===============================================================================
=> SEXY BUTTONS THEME NAMED "<%= file_name %>"
=============================================================================== */
.sexy-button-<%= file_name %> { 
 	background-image:url(bg.png);
	font-size:<%= font_size %>;
}
.sexy-button-<%= file_name %> span { 
	height:<%= height %>; 
	line-height:<%= height %>;
	background-image:url(bg.png);
}